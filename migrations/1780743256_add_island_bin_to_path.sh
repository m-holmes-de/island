echo "Add island bin/ to PATH via environment.d (superseded by envs.lua)"

# This migration is now obsolete — PATH is set via hl.env() in envs.lua.
# Kept for migration state tracking only.
echo "  Skipped (PATH now handled by envs.lua)."
